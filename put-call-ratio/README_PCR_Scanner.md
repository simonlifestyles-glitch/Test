# PCR 市場情緒分析工具

> 輸入股票代碼，即時計算 **Put-Call Ratio（PCR）** 並解讀市場情緒

---

## 一、什麼是 Put/Call Ratio（PCR）？

Put-Call Ratio 是選擇權市場中最常用的**市場情緒指標**之一。

### 計算公式

```
PCR = Put 成交量（或未平倉量）/ Call 成交量（或未平倉量）
```

### 意義

選擇權分為兩種：
- **Call（買權）**：買入的人預期價格**上漲**
- **Put（賣權）**：買入的人預期價格**下跌**

當市場上買 Put 的人越多，PCR 就越高，代表市場情緒越悲觀；反之，買 Call 的人越多，PCR 越低，代表市場情緒越樂觀。

### 評分標準

| PCR 數值 | 市場情緒 | 說明 |
|---|---|---|
| PCR < 0.7 | 樂觀 | Call 主導，市場偏多，需留意過度樂觀的回落風險 |
| PCR 0.7 ~ 1.0 | 中立 | 買賣力道接近，市場不確定性高，方向不明 |
| PCR > 1.0 | 悲觀 | Put 主導，市場偏空，但逆向思考也可能是潛在買點 |

### PCR 作為逆向指標

PCR 常被當作**逆向指標（Contrarian Indicator）**使用：

> 當所有人都在恐慌買 Put，市場往往已接近底部；
> 當所有人都過度樂觀買 Call，市場往往已接近頂部。

這也是為什麼 PCR 在量化交易中常被搭配布林通道使用——突破極端值時，反向操作。

---

## 二、Volume vs Open Interest 的差異

本工具提供兩個版本，分別使用不同的計算基準：

### Volume（成交量）

- **定義**：當天已成交的選擇權合約口數
- **更新頻率**：盤中**即時**更新
- **反映的資訊**：今日市場的**即時情緒與交易活躍度**
- **適合用途**：短線、當日盤中情緒判斷
- **注意事項**：開盤初期成交量少，PCR 數字可能偏差較大，建議在盤中後段查詢

### Open Interest（未平倉量）

- **定義**：目前市場上**尚未平倉**的選擇權合約總口數
- **更新頻率**：**每日收盤後**更新，隔日才反映
- **反映的資訊**：市場參與者的**累積整體部位**
- **適合用途**：中長線、市場整體部位與趨勢判斷
- **注意事項**：資料有一天延遲，盤中數字不會即時變動

### 一句話比較

```
Volume   = 今天大家買賣了多少  →  反應快，但噪音多
OI       = 大家目前累積押了多少 →  反應慢，但訊號更穩定
```

### 對照表

| 項目 | Volume（成交量） | Open Interest（未平倉量） |
|---|---|---|
| 更新頻率 | 即時 | 每日收盤後 |
| 反映時間軸 | 當日短線 | 中長線累積 |
| 噪音程度 | 較高 | 較低 |
| 適合用途 | 盤中快速判斷 | 回測 / 中長線策略 |

---

## 三、程式抓取邏輯

### 資料來源

- **套件**：`yfinance`（Yahoo Finance 免費公開資料）
- **無需 API Key**，無需帳號

### 抓取步驟

```
輸入股票代碼（例如 AAPL）
        ↓
用 yfinance 取得該股所有選擇權到期日列表
        ↓
取最近 3 個到期日（流動性最佳、資料最充足）
        ↓
每個到期日分別抓取 Call 和 Put 的選擇權鏈
        ↓
加總 3 個到期日的 Put 量 / Call 量
        ↓
計算 PCR = 總 Put 量 / 總 Call 量
        ↓
對照評分標準，輸出情緒判斷
```

### 為什麼只取最近 3 個到期日？

- 距到期日越近，選擇權**流動性越好**，成交量和未平倉量數字更具代表性
- 遠月合約往往成交稀疏，加入反而會稀釋訊號

### 程式碼核心邏輯

**Volume 版（`pcr_volume.py`）**
```python
for date in expirations[:3]:
    chain = tk.option_chain(date)
    total_put  += chain.puts['volume'].fillna(0).sum()
    total_call += chain.calls['volume'].fillna(0).sum()

pcr = total_put / total_call
```

**Open Interest 版（`pcr_open_interest.py`）**
```python
for date in expirations[:3]:
    chain = tk.option_chain(date)
    total_put  += chain.puts['openInterest'].fillna(0).sum()
    total_call += chain.calls['openInterest'].fillna(0).sum()

pcr = total_put / total_call
```

---

## 四、檔案說明

| 檔案 | 計算基準 | 適合用途 |
|---|---|---|
| `pcr_volume.py` | 當日成交量（Volume） | 盤中即時情緒判斷 |
| `pcr_open_interest.py` | 未平倉量（Open Interest） | 中長線整體部位判斷 |

---

## 五、安裝與執行

### 安裝套件

```bash
pip install yfinance
```

### 執行

```bash
# 即時情緒版（Volume）
python pcr_volume.py

# 累積部位版（Open Interest）
python pcr_open_interest.py
```

### 執行範例

```
請輸入股票代碼 (例如 AAPL、TSLA、SPY)：AAPL

===============================================
  AAPL - PCR 情緒分析 ( 成交量版 )
===============================================
  計算方式    : 當日成交量 (Volume)
  適合用途    : 盤中即時情緒判斷
  PCR 數值    : 0.8521
  市場情緒    : 中立
  情緒說明    : 市場情緒中立，不確定因素性高
  參考建議    : 觀望為主，等待方向確認後再行動
-----------------------------------------------
  評分依據：
    PCR 0.7 ~ 1.0  ->  中立 (不確定因素高)
    PCR < 0.7      ->  樂觀 (Call 主導)
    PCR > 1.0      ->  悲觀 (Put 主導)
===============================================
```

---

## 六、免責聲明

本工具僅供**學術研究與學習用途**，不構成任何投資建議。
選擇權市場具有高度風險，請自行評估後謹慎操作。
